# Archive

Deprecated or superseded library artifacts may be retained here when historical traceability is useful. Archived files must retain their original metadata, include `status: archived`, and link to the successor artifact when one exists.

Original local source binaries are not stored here by default because the repository is public and publication may expose metadata, sensitive content, or redundant material.
